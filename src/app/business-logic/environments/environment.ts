/* eslint-disable @typescript-eslint/restrict-plus-operands */
import { SERVER_URL, API_URL } from '../../app.constants';

export const environment = {
  serverUrl: SERVER_URL,
  apiUrl: API_URL + 'api/',
};
